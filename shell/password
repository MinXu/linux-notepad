非交互改密码
useradd -s /bin/bash -rmU username
	-s shell
	-r system user
	-m create home dir
	-U create the same name group

userdel -r[f] username
	-r remove the home dir and mail
	-f force remove

echo username:password|chpasswd


交互改密码
adduser username

deluser username

passwd
